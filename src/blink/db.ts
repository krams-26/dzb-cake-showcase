/**
 * Couche données : API REST → MySQL `dzb_cake` (voir server/ et scripts/mysql/).
 * Auth personnel : session cookie via `/api/auth/*` (voir StaffAuthContext).
 */
export { database } from '../data/api-database'
