[build]
  command = "npm run build"
  publish = "dist"

# SPA : toutes les routes → index.html (équivalent à public/_redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
